apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-config
data:
  db_name: gogs
  db_user: gogs
  # delete db_pass once postgres-secret is working properly
  db_pass: gogspassword
  data_dir: /var/lib/postgresql/data/pgdata
